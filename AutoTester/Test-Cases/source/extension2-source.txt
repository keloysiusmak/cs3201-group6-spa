procedure First {
    b = j;
    x = m - u * h * p;
    l = z + l + z + 4 * t * w;
    e = j + z + l;
    if a then {
        if a then {
            if a then {
                if a then {
                    c = 3 + l - g * e + j;
                }
                else {
                    u = a - t - z + f - j + o;
                    m = j * x * k + 45 - z;
                    h = t + 162 * y + o;
                    b = j;
                }
                l = o - a * 170;
            }
            else {
                s = r - g * g + e * o;
                f = u + s - w + y - j - h;
                r = s + e * k + j - i;
                a = l - z * x + b * j;
            }
            e = j - r * y - r - l - o;
            v = d * 170 * p;
        }
        else {
            q = y;
            v = v + q * u * 138 * x;
            b = e * m * b * g * b + z;
        }
        c = b * b - n - z + v;
        y = i + q - f + r * 93 + b;
        h = k + b + v;
    }
    else {
        v = r;
        j = r;
    }
}